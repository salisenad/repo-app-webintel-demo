{
  "name": "first-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "cd frontend && npm run build",
    "intall-client": "cd frontend && npm install",
    "heroku-postbuild": "npm run intall-client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [
    "Senad",
    "Sali"
  ],
  "author": "Senad Sali",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^3.5.5"
  },
  "devDependencies": {
    "webpack-cli": "^3.3.11"
  }
}
